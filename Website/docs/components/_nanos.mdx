import { useActiveVersion } from '@docusaurus/plugin-content-docs/client';

<!-- Getter to get the current version path - for links -->
export const getActiveVersionPath = () => {
	const activeVersion = useActiveVersion();
	return activeVersion.path;
};


<!-- Square Card Link -->
export const CardLink = ({ title, description, href, image_src }) => (
	<a class="card-link" href={href.startsWith("http") ? href : `${getActiveVersionPath()}/${href}`} > 
		<img src={image_src ? image_src : '/img/docs/nanos-world-background.jpg'} />
		<h4>{title}</h4>
		<p>{description}</p>
	</a>
);

<!-- Rectangular Reference Link -->
export const ReferenceLink = ({ children, href }) => (
	<a class="reference-link" href={href.startsWith("http") ? href : `${getActiveVersionPath()}/${href}`}>
		{children}
		<span>
			{href}
		</span>
	</a>
);


<!-- Generic Base Authority component -->
export const BaseAuthority = (img, title) => (
	<a class="authority-availability" href={`${getActiveVersionPath()}/core-concepts/scripting/authority-concepts#methods-and-events-availability`}>
		<img src={img} title={title} />
	</a>
);

<!-- Generic Basic Type component -->
export const BaseType = (label) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/glossary/basic-types#${label.toLowerCase()}`}>{label}</a>
);

<!-- Generic Struct Type component -->
export const BaseStruct = (label, path) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/utility-classes/${path ? path : label.toLowerCase()}`}>{label}</a>
);

<!-- Generic Struct Type component -->
export const BaseClass = (label, path) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/classes/${path ? path : label.toLowerCase()}`}>{label}</a>
);

<!-- Define Enum component -->
export const Enums = ({ children }) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/glossary/enums#${children.toLowerCase()}`} title={`${children} Enum (number)`}>{children}</a>
);

<!-- Define Asset Path component -->
export const BaseAssetPath = (label) => (
	<a href={`${getActiveVersionPath()}/core-concepts/assets#referencing-assets-in-scripting`} title="Asset Path (string)">{label}</a>
);

<!-- Define Special Path component -->
export const SpecialPath = (label) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/glossary/basic-types#specialpath`} title="Special Path (string)">{label}</a>
);


<!-- Defines Authority Types componenets -->
export const AuthorityType = {
	AuthorityOnly: () => BaseAuthority("/img/scripting/authority-only.svg", "authority-only - this can be called only on the side which spawned it!"),
	ServerOnly: () => BaseAuthority("/img/scripting/server-only.svg", "server-only - this can be called only on server side!"),
	ClientOnly: () => BaseAuthority("/img/scripting/client-only.svg", "client-only - this can be called only on client side!"),
	Both: () => BaseAuthority("/img/scripting/both.png", "both-sides - this can be called on any side!"),
};

<!-- Defines Basic Types componenets -->
export const BasicType = {
	Number: () => BaseType("number"),
	String: () => BaseType("string"),
	Boolean: () => BaseType("boolean"),
	Table: () => BaseType("table"),
	Iterator: () => BaseType("iterator"),
	Any: () => BaseType("any"),
	Function: () => BaseType("function"),
	Nil: () => BaseType("nil"),
};

<!-- Defines Struct Types componenets -->
export const Structs = {
	Vector: () => BaseStruct("Vector"),
	Rotator: () => BaseStruct("Rotator"),
	Color: () => BaseStruct("Color"),
	Quat: () => BaseStruct("Quat", "quaternion"),
	Vector2D: () => BaseStruct("Vector2D", "vectortwod"),
};

<!-- Defines Classes componenets -->
export const Classes = {
	Actor: () => BaseClass("Actor", "base-classes/actor"),
	Paintable: () => BaseClass("Paintable", "base-classes/paintable"),
	Pickable: () => BaseClass("Pickable", "base-classes/pickable"),
	Cable: () => BaseClass("Cable"),
	Character: () => BaseClass("Character"),
	File: () => BaseClass("File"),
	Grenade: () => BaseClass("Grenade"),
	Melee: () => BaseClass("Melee"),
	Light: () => BaseClass("Light"),
	Particle: () => BaseClass("Particle"),
	Player: () => BaseClass("Player"),
	Prop: () => BaseClass("Prop"),
	Sound: () => BaseClass("Sound"),
	SceneCapture: () => BaseClass("SceneCapture", "scene-capture"),
	StaticMesh: () => BaseClass("StaticMesh", "static-mesh"),
	TextRender: () => BaseClass("TextRender", "text-render"),
	Trigger: () => BaseClass("Trigger"),
	Vehicle: () => BaseClass("Vehicle"),
	Weapon: () => BaseClass("Weapon"),
	WebUI: () => BaseClass("WebUI"),
};

<!-- Defines Assets Path -->
export const AssetPath = {
	StaticMesh: () => BaseAssetPath("StaticMesh Asset"),
	SkeletalMesh: () => BaseAssetPath("SkeletalMesh Asset"),
	Animation: () => BaseAssetPath("Animation Asset"),
	Particle: () => BaseAssetPath("Particle Asset"),
	Material: () => BaseAssetPath("Material Asset"),
	Map: () => BaseAssetPath("Map Asset"),
	Sound: () => SpecialPath("Sound Asset or Special Path"),
	Image: () => SpecialPath("Image Special Path"),
	HTML: () => SpecialPath("HTML Special Path"),
};