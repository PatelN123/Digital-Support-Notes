window.OlvyConfig = {
  organisation: "dssn",
  target: ".dot__class",
  type: "sidebar",
  view: {
    showSearch: false,
    compact: true,
    showHeader: true, // only applies when widget type is embed. you cannot hide header for modal and sidebar widgets
    showUnreadIndicator: true,
    unreadIndicatorColor: "#5FA0FFFF",
    unreadIndicatorPosition: "center"
  }
};